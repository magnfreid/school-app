# Design handoff status

One row per screen. `Handoff tree` is the git tree SHA of `design/<screen>/`
(`git ls-tree HEAD design/<screen>`, first seven characters) as of when the
row was last updated — fill it in once the handoff is committed.

| Screen | Status | Handoff tree | Plan | Shipped |
|---|---|---|---|---|
| week-view | pending | 71f32ca | — | — |
