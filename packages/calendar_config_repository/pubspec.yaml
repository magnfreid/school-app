name: calendar_config_repository
description: Calendar configuration repository abstraction for the app.
version: 0.1.0
publish_to: "none"

environment:
  sdk: ^3.10.0

resolution: workspace

dependencies:
  freezed_annotation: ^3.0.0

dev_dependencies:
  test: ^1.24.0
  build_runner: ^2.4.0
  freezed: ^3.0.0
  lints: ^6.0.0
