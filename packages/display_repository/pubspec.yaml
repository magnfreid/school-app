name: display_repository
description: Wakelock, screen brightness and external-power repository abstraction for the app.
version: 0.1.0
publish_to: "none"

environment:
  sdk: ^3.12.0
  flutter: ">=3.44.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  battery_plus: ^7.1.1
  screen_brightness: ^2.1.11
  wakelock_plus: ^1.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^6.0.0
